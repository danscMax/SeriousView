<shared:ModalWindow xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shared="using:Tittle.Shared"
        x:Class="Tittle.Features.Help.HelpWindow"
        Title="Справка"
        Width="430" SizeToContent="Height" CanResize="False">
  <!-- Shortcuts reference (ported help modal). Esc / Закрыть dismisses. Chrome = ModalWindow + Border.modalcard. -->
  <Border Classes="modalcard" Padding="22">
    <StackPanel Spacing="12">
      <TextBlock Text="Горячие клавиши" FontSize="16" FontWeight="SemiBold"/>

      <ItemsControl x:Name="ShortcutList">
        <ItemsControl.Styles>
          <Style Selector="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource ChromeForegroundBrush}"/>
          </Style>
          <Style Selector="TextBlock.key">
            <Setter Property="FontFamily" Value="Cascadia Code,Consolas,monospace"/>
            <Setter Property="Foreground" Value="{DynamicResource ChromeForegroundMutedBrush}"/>
            <Setter Property="FontSize" Value="11.5"/>
          </Style>
        </ItemsControl.Styles>
      </ItemsControl>

      <Button Classes="modal-dismiss" Content="Закрыть" HorizontalAlignment="Right"
              AutomationProperties.Name="Закрыть справку" Click="OnCloseClick"/>
    </StackPanel>
  </Border>
</shared:ModalWindow>
